# Product Workflow

<!-- repository-creator:managed:start -->

## 目的

この文書は、新しいサービス・アプリ・Webサイト・ツールなどを作成するときに、

1. まず中心価値を実際に利用できる初期完成版を作る
2. 完成した実物を改めて評価する
3. 機能を拡充する
4. コンテンツを拡充する
5. UI / UX・デザイン品質を向上させる
6. 変更後の実物を再び評価する
7. まだ必要な改善があれば繰り返す

という一連の開発を行うための実行手順を定める。

目的は、単に「動くもの」を作ることではない。

最終的には、機能・コンテンツ・利用体験・デザインを含め、実際に人が使うプロダクトとして十分な完成度まで育てる。

特にデザインについては、AIが短時間で生成したことが分かるような画一的・テンプレート的な見た目をそのまま完成形とせず、人間のプロダクトチームが意図を持って設計・改善したような品質を目指す。

この文書では開発全体の進め方を定める。

各領域固有の詳しい判断基準は、存在する場合は次の専門文書に従う。

- 機能: `FEATURES.md`
- コンテンツ: `CONTENT.md`
- UI / UX・デザイン: `DESIGN.md`
- 完成後の独立したデザインレビュー: `DESIGN_REVIEW.md`
- 将来追加されたその他の専門文書

---

## 使用する場面

次の場合は、このワークフローを使用する。

- 新しいサービス、アプリ、Webサイト、ツールなどを一から作る
- 既存プロダクトを全体的に改善する
- 機能、コンテンツ、UI / UX・デザインなど複数領域を拡充する
- 現在のプロダクトを評価しながら継続的に完成度を上げる

次のように変更内容が限定されている依頼では、このワークフロー全体を実行する必要はない。

- 一つの文言だけ変更する
- 明確に指定された一機能だけ追加する
- 特定の不具合だけ修正する
- 限定された調査だけ行う

ただし、限定された変更からプロダクト全体の問題が明らかになり、依頼が全体改善へ広がった場合は、このワークフローへ移行してよい。

---

## 最重要: ワークフローを途中で終了しない

新規プロダクト作成またはプロダクト全体の改善を依頼された場合、Stage 1、Stage 2、必要な独立レビュー、最終再評価までを一つの連続した作業として扱う。

**Stage 1の完了はタスク完了ではない。**

Stage 1の成立を中間報告してもよいが、その報告を理由に作業を終了してはならない。Stage 1が成立したら、ユーザーの追加指示や通常承認を待たず、同じタスク内で直ちにStage 2へ進む。

同様に、次の状態も最終的なタスク完了ではない。

- 初期完成版が動いた
- ビルドやテストが成功した
- 公開できた
- 一つの機能改善を終えた
- 一つのコンテンツ拡充を終えた
- UI / UX・デザインを一度改善した
- 独立デザインレビューを通過した
- Stage 2を一周した

改善を実装した場合は、変更後のプロダクト全体を新しい実物として再評価する。

全体再評価で1件以上の改善を `採用` した場合、その時点でタスクは未完了である。採用した改善を実装・検証し、再び全体再評価へ戻る。

このサイクルは、**変更後のプロダクト全体を新しく評価した一回のサイクルで、新たに `採用` される改善が0件になるまで継続する。**

途中経過として状況をユーザーへ報告することはできるが、中間報告を作業終了点として扱わない。

例外は、次のような実際のブロッカーが存在する場合だけとする。

- ユーザーにしか決められない重要仕様
- 新しい認証や権限が必要
- 課金が必要
- 外部サービス上の重要な操作が必要
- 取り返しのつかない変更が必要
- 利用可能な手段では続行不能

これらがない限り、「次のStageへ進めますか」「さらに改善しますか」などの通常承認を求めて止まらない。

---

## 基本原則

### 現在の実物を基準に判断する

過去の計画、以前の説明、自分が直前に書いたコードだけを根拠に完成度を判断しない。

利用可能な範囲で、現在の

- コード
- 実画面
- UI
- コンテンツ
- 操作フロー
- 状態変化
- データ
- 実際の動作

を確認し、現在存在するプロダクトを評価する。

以前の判断と現在の実物が食い違う場合は、現在の実物を優先する。

### 候補を考える工程と採用する工程を分ける

改善候補を考えながら、その場で実装を始めてはならない。

まず、不足している可能性のあるものを広く考える。

その後、候補を一つずつ検討し、本当に必要なものだけを選ぶ。

`思いつく → 即実装`

ではなく、

`現状確認 → 候補出し → 精査 → 採否決定 → 実装`

の順で進める。

### 改善数を固定しない

候補数や実装数を事前に固定しない。

「3個追加する」「5個改善する」など、数を満たすことを目的にしてはならない。

評価の結果として、

- 多数の改善が必要
- 数件だけ必要
- 一件だけ必要
- 変更不要

のいずれになってもよい。

### 改善すること自体を目的にしない

すでに良い部分を、改善工程だからという理由で変更してはならない。

追加によって、

- 複雑になる
- 分かりにくくなる
- 情報過多になる
- 維持負担が増える
- 中心価値が弱くなる

のであれば追加しない。

削除、統合、簡略化、現状維持が最善の場合は、それを選ぶ。

### 領域の順序を固定しない

機能拡充、コンテンツ拡充、デザイン品質向上の実施順序は固定しない。

現在のプロダクトの問題、依存関係、変更による影響を確認して、その時点で合理的な順序を決める。

例えば、

`機能 → デザイン → コンテンツ`

でも、

`デザイン → コンテンツ → 機能`

でもよい。

機能変更によって画面構成が変わった場合など、必要であれば同じ領域へ再び戻ってよい。

一つの領域を一回処理したら終了、という考え方はしない。

### ユーザーの通常承認を待たず進める

候補、採否、理由、実装内容は進捗としてユーザーに明示する。

ただし、

- ユーザーにしか決められない仕様
- 新しい認証
- 新しい権限
- 課金
- 外部サービス上の重要な操作
- 取り返しのつかない変更

などが必要でない限り、工程ごとに承認を求めず、そのまま作業を継続する。

---

# Stage 1: 初期完成版を作る

新規プロダクトでは、最初に中心価値を利用できる初期完成版を作る。

この段階では、プロダクト全体の成熟化を同時に行おうとしない。

まず一度、実際に評価できる完成物を作る。

## 1. 依頼と現在の状態を理解する

実装前に、ユーザーの依頼と現在のリポジトリを確認する。

関係する専門文書が存在する場合は読む。

最低限、次を把握する。

- 誰が使うプロダクトか
- ユーザーが何を達成するために使うか
- このプロダクトの中心価値は何か
- 利用開始から中心価値を得るまでの主要な流れは何か
- その流れに不可欠な機能は何か
- その流れに不可欠なコンテンツは何か
- その流れに不可欠なUI / UXは何か

ユーザーが細部まで指定していない場合、合理的に判断できる内容はAIが決める。

細部が未指定であることを理由に、必要以上にユーザーへ質問して作業を止めない。

## 2. 初期完成版を実装する

中心となる利用フローを、入口から結果まで実際に利用できる状態にする。

次のような状態だけでは完成としない。

- トップページだけ存在する
- 見た目だけ作られている
- ボタンが置かれているだけ
- ダミーデータしかない
- ビルドだけ成功する
- HTTP 200が返るだけ
- 主要操作が途中で途切れる
- 中心となる機能が仮実装のまま
- 必須コンテンツが空
- 明らかな壊れた状態を残している

一方、この段階で周辺機能や大量コンテンツを無制限に追加しない。

まず中心価値が成立するところまで完成させる。

## 3. 初期完成版を確認する

今回作った中心フローが成立しているか確認する。

確認方法は変更内容と利用可能な手段に応じて決める。

問題が見つかった場合は、その場で修正して再確認する。

初期完成版の中心価値が実際に成立したことを確認したら、その結果を中間報告としてユーザーへ明示してよい。

**ただし、この報告はタスク完了報告ではない。報告後も作業を止めず、同じタスク内でStage 2へ進む。**

## 4. 初期実装と改善工程を混同しない

Stage 1で自然に行ったUI調整、機能追加、文章修正などを、後述するプロダクト改善工程を実施したものとして扱わない。

初期完成版ができた後、一度実装から離れ、完成した実物を改めて評価する。

ここからStage 2を開始する。

---

# Stage 2: プロダクト改善ループ

Stage 1で完成したプロダクト、または既存のプロダクトを対象として、完成度をさらに高める。

一回の改善だけで終了することを前提にしない。

変更後の実物を再評価し、まだ必要な改善があれば繰り返す。

---

## Step 1: プロダクト全体を確認する

改善実装を始める前に、現在のプロダクト全体を確認する。

この時点では、まだ新しい改善を実装しない。

少なくとも、

- 機能
- コンテンツ
- UI / UX
- デザイン
- 主要フロー
- 主要状態
- Desktop / Mobileなど必要な利用環境

について、現在どのような状態になっているか把握する。

関係する専門文書がある場合は参照する。

---

## Step 2: 今回評価する領域を決める

現在のプロダクトに関係する専門領域を確認する。

少なくとも通常は次を対象とする。

- Feature Expansion
- Content Expansion
- Design Quality Improvement

ただし、順序は固定しない。

現在もっとも大きな問題がある領域や、他の領域の前提になる領域から進めてよい。

将来別の専門文書が追加された場合は、その領域も同じ改善ループへ追加する。

---

# Feature Expansion

機能の追加数を増やすことではなく、プロダクトとして必要な能力を十分にすることを目的とする。

## 1. 現在の機能を確認する

少なくとも次を見る。

- 現在できること
- できないこと
- 主要フロー
- 初回利用
- 繰り返し利用
- 継続利用
- 入力
- 出力
- 保存
- 状態変化
- エラー
- 空状態
- 読み込み状態
- 取消
- 復帰
- データ
- 外部連携

すべてのプロダクトにすべての項目が必要という意味ではない。

そのプロダクトに関係するものだけを評価する。

## 2. 機能候補を広く考える

現在のプロダクトに不足している可能性がある機能を考える。

数は固定しない。

新機能だけでなく、

- 既存機能の改善
- 不足した状態
- 不自然なフロー
- 操作の省略
- 不要な機能の削除
- 重複機能の統合

なども候補に含める。

この時点では、候補を早く絞りすぎない。

## 3. 候補を一つずつ精査する

各候補について少なくとも次を考える。

- 実際のユーザー価値があるか
- 中心価値と関係しているか
- 現在の問題を解決するか
- 既存機能と重複していないか
- 機能過多にならないか
- 操作を複雑にしないか
- 初心者が理解しにくくならないか
- より高度な利用へ自然につながるか
- 実際に利用される可能性があるか
- データや状態が不必要に複雑にならないか
- 既存機能へ自然に接続できるか
- 維持負担が価値に見合うか

検討後、それぞれを

- `採用`
- `見送り`

に分類する。

必要であれば将来検討するものとして `保留` を使用してよい。

採用する理由だけでなく、見送る理由も明示する。

## 4. 実装前に判断結果を出力する

少なくとも次をユーザーへ示す。

- 現在の機能について分かったこと
- 考えた候補
- 各候補の主要な判断理由
- 採用 / 保留 / 見送り
- 今回実装する内容

その後、通常は承認を待たず実装へ進む。

## 5. 採用したものだけ実装する

候補に挙げたものを全部実装しない。

採用したものだけを実装する。

採用が0件なら、機能変更を行わず次へ進む。

## 6. 必要な確認をする

変更した機能と、その変更によって直接影響を受ける部分を確認する。

毎回プロダクト全体を網羅的に操作検証する必要はない。

問題が見つかった場合は修正して再確認する。

この領域の実装と確認だけでタスクを完了せず、`FEATURES.md` の `Workflow Handoff` に従って全体再評価へ戻る。

---

# Content Expansion

コンテンツ数を増やすことではなく、ユーザーへ提供する内容をプロダクトとして十分な状態にすることを目的とする。

コンテンツには例えば次を含む。

- 教材
- 解説
- 記事
- 課題
- シナリオ
- サンプル
- 商品情報
- 施設情報
- テンプレート
- ヘルプ
- 初期データ
- ユーザー向け文章

## 1. 現在のコンテンツを確認する

少なくとも必要に応じて次を見る。

- カバー範囲
- 情報量
- 深さ
- 難易度
- 順序
- 分類
- 重複
- 欠落
- 正確性
- 品質
- 発見しやすさ
- 内容同士のつながり
- 初心者から高度な内容への進行

## 2. コンテンツ候補を広く考える

現在の内容に不足している可能性があるものを考える。

数は固定しない。

追加だけでなく、

- 薄い内容を深くする
- 重複を削除する
- 順序を変更する
- 分類を整理する
- 関連付けを追加する
- 不要な文章を減らす
- 分かりにくい説明を書き直す

なども候補に含める。

## 3. 候補を一つずつ精査する

各候補について少なくとも次を考える。

- ユーザーの目的に必要か
- 中心価値を強めるか
- 既存内容と重複していないか
- 情報過多にならないか
- 単なる量増しになっていないか
- 初心者から高度な内容への流れが自然か
- 現在薄い領域を適切に補うか
- 正確性を維持できるか
- 内容同士の関係が分かりやすくなるか
- 実際の利用体験を改善するか
- 更新や維持が現実的か

それぞれを

- `採用`
- `保留`
- `見送り`

に分類し、理由を明示する。

## 4. 実装前に判断結果を出力する

少なくとも次をユーザーへ示す。

- 現在のコンテンツについて分かったこと
- 考えた候補
- 各候補の主要な判断理由
- 採用 / 保留 / 見送り
- 今回実装する内容

## 5. 採用したものだけ実装する

追加だけを改善と考えない。

必要であれば、

- 追加
- 修正
- 再構成
- 統合
- 削除

を行う。

採用が0件なら変更しない。

## 6. 必要な確認をする

変更内容について必要に応じて、

- 正確性
- 一貫性
- 配置
- 順序
- 他コンテンツとの接続
- 実際の利用フローとの接続

を確認する。

問題があれば修正する。

この領域の実装と確認だけでタスクを完了せず、`CONTENT.md` の `Workflow Handoff` に従って全体再評価へ戻る。

---

# Design Quality Improvement

目的は、単にUIをきれいにすることではない。

現在のプロダクトを、実際に人間のデザイナーやプロダクトチームが意図を持って作り込んだような品質まで近づける。

特に、AI生成プロダクトにありがちな画一性、テンプレート感、安易な構成を残さないことを重視する。

詳細なデザイン原則と参考元の扱いは `DESIGN.md` に従う。

## 1. 現在の実画面を確認する

可能な場合、コードだけで判断せず実画面を見る。

利用可能な場合は、

- ブラウザ
- スクリーンショット
- Vision
- 実際のスマートフォン幅
- 実際のDesktop幅

などを使用する。

必要に応じて主要な状態違いも確認する。

## 2. 必要なら実在する参考プロダクトを調査する

ユーザーが参考元を指定している場合は、それを優先する。

指定がない場合でも、プロダクトレベルのデザイン改善では必要に応じて実在する優れたWebサイトやアプリを調査する。

参考元から学ぶ対象は、色や装飾だけではない。

必要に応じて、

- 情報階層
- 画面構成
- レイアウト
- 版面率
- 余白
- 密度
- タイポグラフィ
- 色彩
- 境界
- 形状
- アイコン
- 画像
- 状態表現
- 操作の見せ方
- ナビゲーション
- モーション
- Desktop / Mobileでの構成変更

などを見る。

表面をコピーせず、このプロダクトに使える判断だけを採用する。

## 3. デザイン改善候補を広く考える

現在のUI / UX・デザインに不足している可能性があるものを考える。

見た目だけではなく、

- 情報構造
- 操作方法
- 認知負荷
- 状態の分かりやすさ
- レスポンシブ
- 視線誘導
- タイポグラフィ
- 密度
- プロダクト固有性

なども対象とする。

## 4. AIらしさを明示的に確認する

少なくとも主要画面について、AI生成物にありがちな次の問題が残っていないか確認する。

- どのサービスにも使えそうな画一的構成
- 理由のないカードの大量使用
- 何でも角丸の箱へ入れる
- 不要な影
- 不要なグラデーション
- 装飾目的だけの背景効果
- 均等すぎる余白
- 均等すぎるレイアウト
- 不自然に整いすぎた対称構成
- 画面全体の情報密度が均一
- 無難すぎるタイポグラフィ
- 巨大な見出しに頼りすぎる
- 意味の薄いキャッチコピー
- genericな説明文
- 不要なアイコン
- 不要なバッジ
- コンポーネントライブラリをそのまま並べたような見た目
- プロダクト名と色を変えれば別サービスとして成立してしまう構成
- MobileがDesktopを単純に縦積みしただけ
- 実際の利用内容より見た目の演出が優先されている

これらを機械的に禁止するわけではない。

目的があり、プロダクトに適しているなら使用してよい。

問題は「理由なくAIがよく使う形へ収束していること」である。

## 5. 候補を一つずつ精査する

各候補について少なくとも次を考える。

- ユーザーが使いやすくなるか
- 初めて使う人が迷いにくくなるか
- 情報の優先順位が明確になるか
- 視線の流れが自然になるか
- 操作方法が理解しやすくなるか
- 現在の状態が理解しやすくなるか
- 既存UIと矛盾しないか
- 不必要に複雑にならないか
- 過剰に装飾的にならないか
- スマートフォンで使いやすいか
- Desktopでも不自然にならないか
- コンテンツの理解を助けるか
- 中心タスクを邪魔しないか
- プロダクト固有の目的に合っているか
- 単なる見た目変更ではなく理由があるか
- AIテンプレート的な画一性を減らせるか
- 参考元の表面的コピーになっていないか

それぞれを

- `採用`
- `保留`
- `見送り`

に分類し、理由を明示する。

## 6. 実装前に判断結果を出力する

少なくとも次をユーザーへ示す。

- 現在のUI / UX・デザインについて分かったこと
- 考えた候補
- 各候補の主要な判断理由
- 採用 / 保留 / 見送り
- 参考元を使った場合は、何を参考にするか
- 参考元から何を採用しないか
- 今回実装する内容

## 7. 採用したものだけ実装する

全面刷新をデフォルトにしない。

現在良い部分は残す。

必要な箇所だけ変更する。

一方、構造そのものが問題であれば、小さな装飾変更だけで済ませず必要な範囲で再設計する。

採用が0件なら変更しない。

## 8. Visual QAを行う

デザイン変更後は、変更内容に応じて必要な実画面を確認する。

必要であれば、

- Mobile
- Desktop
- 主要状態
- 空状態
- エラー状態
- コンテンツ量が異なる状態

などを見る。

毎回すべての画面幅とすべての状態を網羅する必要はない。

問題が見つかった場合は修正し、該当箇所を再確認する。

---

# 独立デザインレビュー

ユーザー向けUIを持つプロダクトでは、Stage 2の改善実装とVisual QAだけをもってデザイン品質確認を完了したことにしない。

機能・コンテンツ・デザインの改善が一通り反映された実物を対象に、利用可能な手段で `DESIGN_REVIEW.md` に従う独立レビューを行う。

このレビューは、直前の実装を正当化するためではなく、実装時とは独立した視点で現在の実画面を再評価するために行う。

レビューで採用すべき修正が見つかった場合は実装・確認する。

**`DESIGN_REVIEW.md` のDelivery Gateを通過しても、プロダクト全体のタスク完了ではない。レビュー後は必ずこの文書の「プロダクト全体の再評価」へ戻る。**

UIを持たないプロダクトなど、独立デザインレビューが対象外である場合は、その理由を明確にして全体再評価へ進む。

---

# その他の専門領域

将来、機能・コンテンツ・デザイン以外の専門文書が追加された場合も、基本的に次の流れを使用する。

`現在の状態を確認する`

↓

`不足している可能性のある候補を広く考える`

↓

`候補を一つずつ精査する`

↓

`採用 / 保留 / 見送りを決める`

↓

`判断結果をユーザーへ示す`

↓

`採用したものだけ実装する`

↓

`変更内容に応じて確認する`

専門領域固有の判断基準は、その専門文書に従う。

その専門領域の処理を終えたことだけでタスクを完了せず、必ずプロダクト全体の再評価へ戻る。

---

# 領域間の行き来

各領域は独立した一回限りの工程ではない。

例えば、

- 機能追加によって画面構成が変わった
- コンテンツ拡充によって情報階層が変わった
- デザイン改善によって機能フローの問題が見つかった
- UI変更によって文章量を再調整する必要が出た

場合は、必要な領域へ戻る。

一度終えた領域へ戻ることを失敗とは考えない。

むしろ、領域間の影響を反映せずに各工程を一度ずつ消化することを避ける。

---

# プロダクト全体の再評価

今回採用した改善の実装と確認が終わったら、変更後のプロダクト全体をもう一度評価する。

この工程は単なる確認ではなく、**次の改善ループを開始するか、ワークフローを終了できるかを判定するための必須工程**である。

このとき、

「先ほど改善したから、もう十分なはず」

とは考えない。

変更後の実物を新しい基準点として見る。

少なくとも現在のプロダクトに関係する次の領域を新しく評価する。

- 機能
- コンテンツ
- UI / UX
- デザイン
- 主要フロー
- 主要状態
- 必要なDesktop / Mobile環境
- 存在するその他の専門領域

各領域について不足している可能性のある具体的な改善候補を検討し、必要な候補を `採用 / 保留 / 見送り` に分類する。

ここで重要なのは、直前に実装した変更だけを見るのではなく、**変更後のプロダクト全体を改めて見ること**である。

### 継続判定

全体再評価で新たに1件以上の改善が `採用` された場合、タスクは未完了である。

その場合は最終報告を行わず、採用された改善について適切な専門領域へ戻り、実装・検証する。その後、必要な独立レビューを経て、再びプロダクト全体の再評価を行う。

### 収束判定

変更後のプロダクト全体を新しく評価した一回のサイクルで、**新たに `採用` される改善が0件だった場合**、改善ループは収束したと判断できる。

`保留` や `見送り` が存在すること自体は未完了を意味しない。

将来あれば便利なもの、好みの差に留まるもの、価値より複雑化や維持負担が大きいものは、適切に保留または見送りとしてよい。

---

# 終了条件

固定回数で終了しない。

「一周したから終了」
「3回改善したから終了」
「かなり良くなったから終了」

とはしない。

次の状態をすべて満たした場合にのみ完了とする。

- 中心となる利用体験が成立している
- 現在関係する専門領域を確認した
- 不足している可能性のある候補を十分に検討した
- 候補をそのまま実装せず精査した
- 採用した改善は実装した
- 変更内容について必要な確認を行った
- UIを持つ場合は必要な独立デザインレビューを行った
- 変更後のプロダクト全体を新しい基準点として再評価した
- その最新の全体再評価サイクルで、新たに `採用` される改善が0件だった

### 完了として扱ってはならない状態

次だけでは終了条件を満たさない。

- Stage 1が完了した
- 初期完成版が公開できた
- ビルドやテストが成功した
- 機能改善が完了した
- コンテンツ拡充が完了した
- デザイン修正が完了した
- 独立デザインレビューが完了した
- Stage 2を一周した
- 直前に挙げた改善をすべて実装した

これらの後には、まだ最新状態のプロダクト全体再評価が必要である。

改善候補自体を一切思いつかなくなる必要はない。

将来的には有用かもしれないもの、好みでしかないもの、価値より複雑化が大きいものまで実装する必要はない。

**現在のプロダクト全体を新しく評価した結果、今実装する合理的な理由がある改善の `採用` が0件になった状態**を終了条件とする。

---

# 進捗の出力

作業中の内部思考をすべて公開する必要はない。

ただし、ユーザーが「なぜその変更になったのか」を追えるようにする。

各改善領域では、実装前に少なくとも次を出力する。

1. 現在の状態について分かったこと
2. 改善候補
3. 各候補を評価した主要な理由
4. 採用 / 保留 / 見送り
5. これから実装する内容

実装後は少なくとも次を出力する。

1. 実際に変更した内容
2. 確認した内容
3. 問題が見つかった場合は修正した内容
4. 次にどの領域を見るか、または全体再評価へ進むこと

これらは中間報告である。**中間報告を出したことを理由に作業を停止しない。**

最終的に作業完了と報告できるのは、この文書の終了条件を満たした後だけである。

結論だけを突然提示せず、重要な判断結果をユーザーが確認できる状態で進める。

---

# 記録

今回限りの

- 候補一覧
- 採否一覧
- 検討履歴
- 作業経過
- 確認結果

を保存するためだけに、新しい恒常ファイルを作らない。

これらは作業中の進捗として扱う。

長期的に維持すべき、

- プロダクト固有の仕様
- 重要な設計判断
- 品質基準
- 継続して守るべきルール
- 参考元から採用した恒常的な判断

だけを、対応する専門文書のプロジェクト固有部分へ反映する。

リポジトリ内の文書とファイルは必要最小限に保つ。

<!-- repository-creator:managed:end -->
