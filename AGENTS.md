# Project Instructions

<!-- repository-creator:managed:start -->

- 作業前に依頼と現在の実装を確認し、存在する文書のうち関係するものだけを読む。
- 新規サービスの一気通貫開発、またはコア完成後の品質向上・拡充では、最初に `WORKFLOW.md` を読む。工程を圧縮せず、前段階の完了確認後に次へ進む。
- 機能、フロー、状態、ルール、データ、外部連携を扱う場合は `FEATURES.md` を読む。
- サービス固有のコンテンツ、その範囲・深さ・構造・品質、ユーザー向け文言を扱う場合は `CONTENT.md` を読む。
- Design / UI / UX、視覚表現、操作方法、実在プロダクトのデザイン参照を扱う場合は、実装前に `DESIGN.md` を読む。
- ユーザー向けデザインの実装完了後に独立デザインレビューを行う場合は `DESIGN_REVIEW.md` を読む。制作時の常設ルールとしては使わない。
- 複数領域にまたがる場合は該当文書を併読する。何が起きるかは `FEATURES.md`、どんな中身を提供するかは `CONTENT.md`、どう見せて操作させるかは `DESIGN.md` の責務とする。
- 関係しない文書は読まず、存在しない専門文書を空のまま追加しない。
- 恒常的な判断が変わった場合だけ、該当する専門文書の管理マーカー外にあるプロジェクト固有部分を更新する。管理マーカー内は編集しない。
- 確認は変更内容に応じた必要最小限とする。Desktop / Mobileの両方で一連の操作を通す網羅的な検証をデフォルトでは行わない。デザイン作業では必要に応じて実画面やスクリーンショットで見た目を確認する。
- ローカルとGitHubの全ファイルをハッシュで照合したり、完全一致を確認したりする作業はデフォルトでは行わない。競合、同期不整合、反映漏れが疑われる場合だけ必要な範囲で確認する。
- リポジトリ構成、依存関係、恒常文書は必要最小限に保つ。

<!-- repository-creator:managed:end -->

<!-- repository-creator:completion:start -->
## 必須作業と完了条件

- コードを書き終えただけで完了にしない。依頼の実装 → 必須作業 → 関連テスト・検査 → エラーの原因を修正 → 再検査を、対象項目がすべて成功するまで繰り返す。検査を削除・緩和したり、失敗を無視して終了コードを0にしたりしない。
- 作業開始前のコミットSHAを記録する。再試行でも同じSHAを基準にし、1つの依頼・作業単位で正式バージョンを1回だけ増やす。変更なしなら増やさない。コード以外の設定・文書の変更も同じ作業単位に含める。
- 正式バージョンは `v1`、`v2`、`v3`…とする。現在の正式表示が `vN` なら引き継ぐ。日付形式・不明な番号からの移行時だけ `v1` から開始する。文章中の「v1」、変更履歴、APIの `/v2`、保存データのスキーマ、依存パッケージのバージョンを製品バージョンと判断して置換しない。
- 正本は既存UIまたは既存の明示されたバージョン定数に置く。新たなVERSIONファイル・管理フォルダを増やさない。既存の生成処理・VERSIONファイルがある場合は、その仕組みと表示の整合を保ち、生成物だけを修正しない。UIにはその画面と同じ成果物に埋め込んだ `vN` を表示する。
- `.github/mandatory.py` がある場合は、作業開始SHAを `BASE_SHA` として `python3 .github/mandatory.py --base "$BASE_SHA" --fix` を実行し、続けて `--fix` なしでも合格を確認する。既存のテスト・ビルド・同期検査も別途実行する。必須作業の追加は、可能ならこのスクリプトのTASKSへ追加し、自動化できない項目は本節で完了条件を明示する。
- GitHub Pages対象のサイトは、既存の公開方式を維持し、変更反映後の実URLを開いて確認する。新しいソフトウェアリポジトリの作成ではPages公開とURLの最終チャット報告までを必須とする。既存リポジトリでもサイト変更時はPages URL・公開確認結果・現在のvNを報告する。非公開コード・個人情報・GASの認証情報を公開して条件を満たそうとしない。
- デフォルトブランチ以外で作業した場合は、commit・push後の成果物に対応するGitHack URLとvNを最終チャットに必ず記載する。公開済みの静的HTMLに限り `https://raw.githack.com/<owner>/<repo>/<commit SHA>/<入口HTMLのパス>` を使用できる。実際に開いて確認し、未push・未ビルド・非公開リポジトリ等で表示できないURLを「確認済み」と報告しない。表示不可ならその制約と利用可能な確認方法を明示し、勝手に公開範囲を変えない。
- GASを新規作成・設定する場合、保存元アカウントにかかわらず、非公開 `gakkii415/repository-creator` のAGENTS.mdに定めた固定共有先へ編集権限で共有する。今後追加されるアカウントにも同じ条件を適用する。独立GASはプロジェクト、バインドGASは親ファイルが対象。共有後に権限を再取得して確認し、保存先・所有者・トリガーの実行者は勝手に変えない。共有先の私用アドレスや認証情報を公開リポジトリに転記しない。
- 既存のブランチ保護・PR必須・本番同期チェック・レビュー必須条件を優先して守る。共有権限不足、認証不足、保護設定、ユーザー判断が必要な場合は迂回せず、未完了の項目と理由を報告する。「ローカル検査合格」「GitHub反映」「公開反映」「GAS共有」「最終報告」を混同しない。

<!-- repository-creator:completion:end -->
